%% vim: ts=4 sw=4 et ft=erlang
%%  -*- mode: erlang -*-
%%
%% Please node, this particular file is completely commented out because the
%% inets environment gets generated by default by the simple_bridge.config. But
%% if you do wish to initialize an inets environment, specifying *any* of these
%% rules will immediately disable the automatic inets config generation.
%%
[{inets, [
%%    {services, [
%%        {httpd, [
%%			{port, 8000},
%%			{bind_address, {0,0,0,0}},
%%			{server_name, "simple_bridge"},
%%			{server_root, "."},
%%			{document_root, "./priv/static"},
%%			{error_log, "./log/inets.log"}, 
%%			{modules, [inets_simple_bridge_anchor]},
%%
%%			{mime_types, [
%%				{"css", "text/css"}, 
%%				{"js", "text/javascript"}, 
%%				{"html", "text/html"}
%%			]}
%%        ]}
%%    ]}
]}].
